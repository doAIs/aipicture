# FastAPI Backend Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# CORS and Security
python-jose[cryptography]>=3.3.0

# File handling
aiofiles>=23.2.1
python-magic-bin>=0.4.14;sys_platform=="win32"
python-magic>=0.4.27;sys_platform!="win32"

# Image/Video processing
Pillow>=10.0.0
opencv-python>=4.8.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# AI/ML dependencies (shared with main project)
torch>=2.0.0
transformers>=4.35.0
diffusers>=0.24.0
accelerate>=0.24.0
peft>=0.7.0
ultralytics>=8.0.0

# Face recognition
face-recognition>=1.3.0
dlib>=19.24.0

# Audio processing - Speech Recognition
openai-whisper>=20231117

# Text-to-Speech Engines (Multiple options for different use cases)
# Option 1: Edge TTS (Free, Microsoft voices, 100+ languages)
edge-tts>=6.1.12  # Quick deployment, no auth

# Option 2: CosyVoice (Advanced voice cloning, Chinese优化)
# CosyVoice requires manual installation:
# git clone https://github.com/FunAudioLLM/CosyVoice.git
# pip install -r CosyVoice/requirements.txt

# Option 3: GPT-SoVITS (Zero-shot voice cloning)
# GPT-SoVITS requires manual installation:
# git clone https://github.com/RVC-Boss/GPT-SoVITS.git
# See GPT-SoVITS/requirements.txt for dependencies

# Common TTS utilities
gTTS>=2.5.0  # Fallback for simple scenarios
pyttsx3>=2.90  # Offline TTS

# Lip-sync and facial animation
# Wav2Lip for lip-sync video generation
torch>=2.0.0  # Already included in AI/ML section
torchvision>=0.15.0
torchaudio>=2.0.0
librosa>=0.10.0  # Audio feature extraction
scipy>=1.11.0  # Audio processing
soundfile>=0.12.1  # Audio I/O for alignment
ffmpeg-python>=0.2.0  # Video processing automation

# Real-time voice chat and conversational AI (Future integration)
# sounddevice>=0.4.6  # Real-time audio I/O
# pyaudio>=0.2.13  # Alternative audio I/O
webrtcvad>=2.0.10  # Voice activity detection for real-time chat

# Training utilities
datasets>=2.14.0
evaluate>=0.4.0
tensorboard>=2.15.0

# Utils
numpy>=1.24.0
tqdm>=4.66.0
